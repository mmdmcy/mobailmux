[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mobailmux"
version = "0.1.0"
description = "Mobile control slots for CLI AI agents."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
  "requests>=2.31",
]

[project.scripts]
mobailmux = "mobailmux.cli:main"
mobailmux-web = "mobailmux.web:main"

[tool.setuptools.packages.find]
where = ["src"]
